FROM python:3.11-slim

WORKDIR /app

# dipendenze leggere (no GPU)
RUN pip install --no-cache-dir nicegui httpx

# copia solo il pacchetto frontend
COPY frontend/ ./frontend/
COPY run_frontend.py .

EXPOSE 8080

CMD ["python3", "run_frontend.py"]
